namespace AssetStudioExporter
{
    public enum ImageFormat
    {
        Auto = 0,

        Png = 1,// 默认
        Jpeg,
        Webp,
        Tga,
        Tiff,
        Bmp,
        Gif
    }

    public enum AudioFormat
    {
        Auto = 0,

        Wav = 1,// 默认
        Ogg,
        Mp3
    }
}
